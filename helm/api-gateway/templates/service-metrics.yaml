apiVersion: v1
kind: Service
metadata:
  name: api-gateway-metrics
spec:
  selector:
    app: api-gateway
  ports:
    - name: management
      port: {{ .Values.managementPort }}
      targetPort: management
  type: ClusterIP
